<%= react_component('events/form/event_form', { event: @event,
                                                organizations: @organizations,
                                                places: @places }) %>
